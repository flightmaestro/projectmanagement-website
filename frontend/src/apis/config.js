const HOST = 'http://localhost'
const PORT = '5000'

const ROOT = `${HOST}:${PORT}`

export const eventsUrl = `${ROOT}/events`
export const projectsUrl = `${ROOT}/projects`
export const tasksUrl = `${ROOT}/tasks`
export const taskStatusesUrl = `${ROOT}/taskStatuses`
export const labelsUrl = `${ROOT}/labels`
export const usersUrl = `${ROOT}/users`
export const costsUrl = `${ROOT}/costs`
export const authUrl = `${ROOT}/auth`